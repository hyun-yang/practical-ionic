import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'extra_ch06_fb_firestore_paging_crud',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
